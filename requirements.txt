# personal-agent Phase 1 依賴

# Telegram
python-telegram-bot==21.9

# LLM
groq==0.13.0
httpx==0.27.2          # Ollama HTTP 客戶端

# 設定
python-dotenv==1.0.1
pyyaml==6.0.2

# 後續 Phase 預留（先安裝好，不影響 Phase 1）
# chromadb==0.5.23       # Phase 4 記憶系統
# playwright==1.49.1     # Phase 3 瀏覽器
# google-auth-oauthlib   # Phase 5 Gmail
# apscheduler==4.0.0a4   # Phase 5 排程
